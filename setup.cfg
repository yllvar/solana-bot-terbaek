[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    --cov=solana_bot
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    -v
    --tb=short
